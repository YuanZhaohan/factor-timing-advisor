interface:
  display_name: "因子择时顾问"
  short_description: "冻结历史并追加式一键更新择时信号、复合策略、图表和网页报告"
  default_prompt: "使用 $factor-timing-advisor 运行唯一的追加式 update 命令，等待自然完成后根据 update_status.json 汇报新增日期、被忽略的历史差异和最新信号。"
